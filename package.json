{
  "name": "transaction-simulation",
  "version": "1.0.0",
  "description": "workbench for transactions in mongodb",
  "main": "index.js",
  "scripts": {
    "test": "DOTENV_CONFIG_PATH=envs/mongo_replica/.env jest --setupFiles dotenv/config",
    "test_single_describe": "DOTENV_CONFIG_PATH=envs/mongo_replica/.env jest --detectOpenHandles --setupFiles dotenv/config -t",
    "start_replicaSet": "node -r dotenv/config index.js dotenv_config_path=envs/mongo_replica/.env"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marco-luzzara/mongodb-transaction-simulation.git"
  },
  "keywords": [
    "mongodb",
    "transactions"
  ],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/marco-luzzara/mongodb-transaction-simulation/issues"
  },
  "homepage": "https://github.com/marco-luzzara/mongodb-transaction-simulation#readme",
  "dependencies": {
    "dotenv": "^8.2.0",
    "mongodb": "^3.6.4"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
